# 💰 Personal Finance Tracker API

REST API сервис для учета личных финансов. Позволяет управлять транзакциями, работать с категориями расходов и получать сводную аналитику.

Проект полностью контейнеризирован с использованием **Docker**. Реализована автоматическая система миграций БД и изолированное окружение для тестов.

---

## 🛠 Технологический стек

*   **Язык:** Python 3.10
*   **Фреймворк:** FastAPI
*   **База данных:** PostgreSQL 15
*   **ORM:** SQLAlchemy (работа с БД)
*   **Миграции:** Alembic (автоматическое управление схемой БД)
*   **Валидация:** Pydantic V2 (строгая типизация и валидаторы данных)
*   **Тестирование:** Pytest (интеграционные тесты с отдельной тестовой БД)
*   **Контейнеризация:** Docker & Docker Compose

---

## ✨ Ключевые особенности

1.  **Автоматические миграции:** При запуске контейнера Alembic автоматически проверяет и обновляет схему базы данных. Ручное вмешательство не требуется.
2.  **Валидация данных:**
    *   Названия категорий автоматически приводятся к формату Title Case (например, "еда" -> "Еда").
    *   Запрещено использование цифр в названиях категорий.
    *   Проверка на существование внешних ключей (Foreign Keys).
3.  **Изолированное тестирование:** Тесты запускаются в отдельном Docker-контейнере и используют отдельную базу данных (`test_db`), чтобы не пересекаться с основными данными.
4.  **Healthchecks:** Контейнеры приложения и тестов ожидают полной готовности базы данных перед стартом.

---

## 🚀 Как запустить проект

Для запуска вам понадобятся установленные **Docker** и **Docker Compose**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <ссылка-на-ваш-репозиторий>
    cd FinanceTracker
    ```

2.  **Запустите проект одной командой:**
    ```bash
    docker-compose up --build -d
    ```

    *Эта команда соберет образы, запустит PostgreSQL, применит миграции (Alembic) и запустит сервер.*


3.  **Откройте документацию:**
    После запуска API доступно по адресу:
    👉 **http://localhost:8000/docs** (Swagger UI)

---

## 🧪 Тестирование

Проект содержит набор интеграционных тестов, покрывающих CRUD-операции, валидацию и логику сводной статистики.

Тесты запускаются в изолированном контейнере.

**Запуск тестов:**
```bash
docker-compose run --rm tests
```

*После выполнения тестов контейнер автоматически удалится, не оставляя мусора.*

---

## 📂 Структура проекта

```text
FinanceTracker/
├── app/
│   ├── main.py          # Точка входа, эндпоинты API
│   ├── models.py        # SQLAlchemy модели (Таблицы БД)
│   ├── schemas.py       # Pydantic схемы (Валидация)
│   └── database.py      # Настройка подключения к БД
├── alembic/             # Файлы миграций
├── tests/               # Тесты (Pytest)
├── docker-compose.yml   # Оркестрация контейнеров (App, DB, Tests)
├── Dockerfile           # Инструкция сборки образа
└── requirements.txt     # Зависимости
```

---

## 🔌 Основные Эндпоинты

### Категории
*   `GET /categories/` — Список всех категорий.
*   `POST /categories/` — Создание новой категории (с валидацией имени).

### Транзакции
*   `POST /transactions/` — Добавление дохода/расхода (по ID категории).
*   `GET /transactions/` — Список транзакций (с фильтрацией по категории и пагинацией).
*   `GET /transactions/{id}` — Детальная информация о транзакции.
*   `PUT /transactions/{id}` — Обновление данных транзакции.
*   `DELETE /transactions/{id}` — Удаление транзакции.

### Аналитика
*   `GET /transactions/summary/` — Сводка расходов по категориям (общая сумма).